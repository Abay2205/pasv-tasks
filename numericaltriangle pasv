function numericalTriangle(n){
  let str = '';
  for(let i = 1; i <= n; i++){
    for(let j = 1; j <= i; j++){
      if(j === i) str = str + i + '';
      else str = str + i + ' ';
    }
    if(i !== n) str = str + '\n';
  }
  return str;
}
console.log(numericalTriangle(7));
